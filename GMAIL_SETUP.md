# Gmail 알림 기능 설정 가이드

## 📧 개요

구매신청이 제출되면 자동으로 관리자의 Gmail로 알림 이메일이 전송됩니다.

## ✨ 기능

구매신청 제출 시 다음 내용이 포함된 이메일이 관리자에게 자동 발송됩니다:

1. **신청자 정보**
   - 이름
   - 소속
   - 이메일

2. **구매 정보**
   - 장비/품목명
   - 수량
   - 장소
   - 용도
   - 기타사항

3. **첨부 사진** (있는 경우)
   - 최대 3장까지 이메일에 직접 표시
   - 나머지는 개수 안내

## 🔧 설정 방법

### 1. 관리자 이메일 설정

`AppConfig.kt` 파일에서 관리자 이메일 주소를 설정하세요:

```kotlin
object AppConfig {
    // 📧 관리자 이메일 주소 (Gmail로 구매신청 알림 수신)
    const val MANAGER_EMAIL = "your-admin-email@gmail.com"  // 이 주소로 변경
}
```

### 2. Gmail API 권한

앱에서 Gmail을 통해 이메일을 전송하려면 다음 권한이 필요합니다:

- **Gmail.SEND**: 사용자를 대신하여 이메일 전송

이 권한은 Google 로그인 시 자동으로 요청됩니다. 사용자는 첫 로그인 시 다음과 같은 동의 화면을 보게 됩니다:

```
구매신청 앱이 다음을 요청합니다:
✓ 이메일 주소 확인
✓ Gmail을 사용하여 이메일 전송
```

### 3. Google Cloud Console 설정

Gmail API가 작동하려면 Google Cloud Console에서 다음 설정이 필요합니다:

#### 3.1 Gmail API 활성화

1. [Google Cloud Console](https://console.cloud.google.com/) 접속
2. 프로젝트 선택
3. **API 및 서비스** > **라이브러리** 이동
4. "Gmail API" 검색
5. **사용 설정** 클릭

#### 3.2 OAuth 동의 화면 설정

1. **API 및 서비스** > **OAuth 동의 화면**
2. 사용자 유형 선택 (내부/외부)
3. 앱 정보 입력:
   - 앱 이름: `구매신청 관리`
   - 사용자 지원 이메일
   - 개발자 연락처 정보
4. **범위 추가 또는 삭제** 클릭
5. 다음 범위 추가:
   - `https://www.googleapis.com/auth/gmail.send`
   - `https://www.googleapis.com/auth/userinfo.email`
6. 저장 및 계속

#### 3.3 OAuth 클라이언트 ID 확인

1. **API 및 서비스** > **사용자 인증 정보**
2. OAuth 2.0 클라이언트 ID 확인
3. 앱의 `google-services.json`에 반영되어 있는지 확인

## 📝 사용 방법

### 일반 사용자

1. 앱에 Google 계정으로 로그인
   - 첫 로그인 시 Gmail 전송 권한 동의 필요
   
2. 구매신청 작성 및 제출
   - 모든 필수 정보 입력
   - 사진 첨부 (선택)
   
3. 제출 완료
   - Firestore에 저장
   - Google Sheets에 백업
   - **Gmail로 관리자에게 알림 전송** ← 새 기능!
   - FCM 푸시 알림 발송

### 관리자

1. 이메일 수신
   - `MANAGER_EMAIL`로 설정한 주소로 알림 수신
   - 제목: "알림: 구매신청 도착"
   
2. 이메일 확인
   - 신청자 정보
   - 구매 정보
   - 첨부 사진 (있는 경우)
   
3. 앱에서 처리
   - 앱을 열어 승인/거부 처리

## 🔍 이메일 샘플

```
┌─────────────────────────────────────────┐
│  🔔 새로운 구매신청이 도착했습니다     │
│  신청일시: 2024-11-11 12:30:00         │
└─────────────────────────────────────────┘

[대기중]

👤 신청자 정보
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
이름:     김철수
소속:     구매팀
이메일:   user@example.com

🛒 구매 정보
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
장비/품목명: 노트북
수량:       1
장소:       사무실
용도:       업무용
기타사항:   긴급 구매 건

📷 첨부된 사진
[사진 이미지 1]
[사진 이미지 2]

📱 앱에서 확인하기:
구매신청 관리 앱을 열어 자세한 내용을
확인하고 승인/거부 처리를 진행해주세요.
```

## 🚨 문제 해결

### Gmail 이메일이 전송되지 않는 경우

1. **Gmail API 활성화 확인**
   - Google Cloud Console에서 Gmail API가 활성화되어 있는지 확인
   
2. **OAuth 권한 확인**
   - 앱을 삭제하고 재설치한 후 다시 로그인
   - Gmail 전송 권한에 동의했는지 확인
   
3. **관리자 이메일 확인**
   - `AppConfig.MANAGER_EMAIL`이 올바른지 확인
   - Gmail 주소여야 함 (다른 이메일 서비스는 지원 안 됨)
   
4. **로그 확인**
   ```bash
   adb logcat | grep GmailHelper
   ```
   - "Gmail service initialized successfully" 메시지 확인
   - 오류 메시지 확인

### 권한 오류

만약 다음과 같은 오류가 발생하면:
```
403 Forbidden: Request had insufficient authentication scopes
```

**해결 방법:**
1. 앱 데이터 삭제
2. Google 계정에서 앱 권한 취소
   - https://myaccount.google.com/permissions 접속
   - "구매신청 관리" 앱 찾기
   - **액세스 권한 삭제** 클릭
3. 앱에서 다시 로그인
4. Gmail 전송 권한에 동의

### 이메일 전송 실패해도 앱은 정상 작동

Gmail 전송이 실패하더라도:
- ✅ Firestore에 데이터 저장됨
- ✅ Google Sheets에 백업됨
- ✅ FCM 푸시 알림 발송됨
- ✅ 구매신청은 정상 처리됨

Gmail은 **추가 알림 수단**이므로 실패해도 앱 기능에는 영향이 없습니다.

## 📊 동작 흐름

```
사용자 구매신청 제출
        ↓
1. Firestore 저장
        ↓
2. Google Sheets 백업
        ↓
3. Gmail 알림 전송 ← 새 기능
   ↓           ↘
 성공         실패 (로그 기록, 계속 진행)
   ↓
4. FCM 푸시 알림
        ↓
5. 사용자에게 성공 메시지
```

## 🔐 보안 고려사항

1. **OAuth 2.0 인증**
   - Google의 표준 OAuth 2.0 프로토콜 사용
   - 토큰은 안전하게 저장됨
   
2. **최소 권한 원칙**
   - Gmail.SEND 권한만 요청 (이메일 읽기 권한 없음)
   - 사용자의 이메일을 읽거나 수정할 수 없음
   
3. **사용자 동의**
   - 모든 권한은 사용자의 명시적 동의가 필요
   - 언제든지 권한 취소 가능

## 📞 지원

문제가 계속되면:
- 로그 파일 확인: `adb logcat | grep "PurchaseRequest\|GmailHelper"`
- Firebase Console에서 Crashlytics 확인
- 시스템 관리자에게 문의

---

**버전**: 1.1  
**업데이트**: 2024-11-11  
**작성자**: GitHub Copilot
